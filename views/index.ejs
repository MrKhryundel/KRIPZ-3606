<div class="jumbotron">
    <h1 class="display-4">Ласкаво просимо до нашого магазину</h1>
    <p class="lead">Знайдіть найкращі продукти за вигідними цінами!</p>
    <hr class="my-4">
    <p>Перегляньте наш асортимент свіжих та смачних продуктів.</p>
    <a class="btn btn-primary btn-lg" href="/products" role="button">Перейти до покупок</a>
</div>

<div class="row">
    <% if (products && products.length > 0) { %>
        <% products.forEach(function(product) { %>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <img src="<%= product.image %>" class="card-img-top" alt="<%= product.name %>">
                    <div class="card-body">
                        <h5 class="card-title"><%= product.name %></h5>
                        <p class="card-text"><%= product.description %></p>
                        <p class="card-text"><strong>Ціна: <%= product.price %> ₴</strong></p>
                        <a href="/products/<%= product._id %>" class="btn btn-primary">Детальніше</a>
                    </div>
                </div>
            </div>
        <% }); %>
    <% } else { %>
        <div class="col-12">
            <p>No products available.</p>
        </div>
    <% } %>
</div>
